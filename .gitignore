# Go
/pan-agent
/pan-agent.exe
/pan-agent-*.exe
/pan.exe
/sha-stamp.exe
*.exe~
*.test
/coverage/

# Claw3D vendored upstream clone — cloned on demand by scripts/build-bundle.sh
# from the pinned SHA in internal/claw3d/vendor/upstream.sha. The compiled
# bundle lives at internal/claw3d/bundle/ and IS committed.
internal/claw3d/vendor/upstream/

# Tooling caches — Claude Code session data, octo orchestration results
.claude/
.claude-octopus/

# Ruflo / claude-flow runtime — agent swarms, vector DB, daemon state
.claude-flow/
.swarm/
ruvector.db

# Per-user tooling state — MCP project config (auto-generated), Playwright MCP cache,
# remember-skill buffers, impeccable design notes
.mcp.json
.playwright-mcp/
.remember/
.impeccable.md

# Tauri / Rust
desktop/src-tauri/target/
desktop/src-tauri/gen/

# Tauri sidecar binaries — built fresh by CI (`go build -o ...`), not tracked
desktop/src-tauri/binaries/pan-agent-*
!desktop/src-tauri/binaries/.gitkeep

# Node
desktop/node_modules/
desktop/dist/

# Signing keys
*.key
!*.key.pub

# OS
.DS_Store
Thumbs.db
build/
dist/
